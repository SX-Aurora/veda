from setuptools import setup, find_packages, Extension
import os

packages = find_packages()

setup(
	name			= 'veda',
	version			= '${VEDA_VERSION}',
	package_dir		= {'veda': 'veda'},
	packages		= packages,
	include_package_data	= True,
	python_requires		= '>=3.5',
)